# 后端最新修复迁移与动态验收计划

## 1. 当前状态和结果

- 状态：已结束。
- 结果：已完成；本机开发库和隔离测试库已升级至 `20260923_02`，恢复演练、完整 Backend 动态测试和覆盖率门禁通过，测试库已恢复到测试前基线。
- 范围：本机开发库和隔离测试库的最新迁移、Backend 动态测试、范围内失败修复、文档同步及本地 Git 提交。

## 2. 关联需求

BASE-DB-001、BASE-COM-204、BASE-QUAL-001。保持既有退款资金事实与内部补偿分层，不新增产品能力。

## 3. 背景与目标

阶段 A 至 F 的本地修复已有源码，既有动态证据止于 `20260922_07`。本任务核实并升级至当前迁移 head，执行完整 Backend pytest 与 90% 覆盖率门禁，修复验证发现的范围内问题，并将实际结果与此前文档勘误一并本地提交。

## 4. 授权与非目标

用户在本会话明确要求“请执行数据库迁移、动态测试或 Git 提交”。结合前文待执行项，本任务执行本机数据库迁移、Backend 动态验证及本地 Git 提交。允许必要备份与独立测试库恢复、失败诊断和相关修复；不涉及生产、真实资金渠道、前端测试、推送、PR、发布或部署。

## 5. 规则、方案与实施顺序

| 来源 | 约束 | 落实与验证 |
| --- | --- | --- |
| Backend AGENTS、工程标准与测试策略 | PostgreSQL 与 Redis 隔离，失败不跳过，不降低覆盖率 | 核对本机开发库、独立 `_test` 库和 Redis 15；完整 pytest |
| 备份恢复手册 | 不可逆迁移先验证备份及恢复，不覆盖原库 | 迁移前备份，独立恢复库比对 revision、表、行数和约束；保留加密开发库备份 |
| 根规则与开发流程第 17 节 | 精确补丁、保护既有修改、失败先诊断 | 保留现有四份文档改动，逐文件修复与差异复读 |
| 数据字典 8.6 节 | 资金成功独立持久化，补偿完成后申请 completed | 检查并动态验证现有退款与恢复用例，completed_at 保留渠道确认时间 |

1. 核对数据库、服务、已有缓存、迁移图及当前 revision，禁止输出凭据。
2. 修复静态审查发现的迁移前置检查顺序问题；已部署 revision 不改写，操作前核实本机状态及仓库部署证据。
3. 先验证备份恢复，再执行已有库增量、重复升级、隔离空库升级与 Alembic 漂移检查。
4. 运行完整 Backend pytest、PostgreSQL/Redis 集成及覆盖率门禁；按证据修复失败后复验。
5. 执行 Backend 轻量检查及受影响文档治理门禁，更新实际状态、验证证据和剩余边界。
6. 清理本次创建的测试资源，复读最终差异，精确暂存并创建本地提交。

## 6. 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 涉及 | 动态验证与范围内失败修复 | 工程标准与当前实现 | pytest、覆盖率及 Backend 轻量门禁 |
| Admin | 不涉及 | 不改页面 | 无 | 不运行前端测试 |
| Web | 不涉及 | 永久冻结 | 无 | 不启动服务 |
| API Client | 条件性涉及 | 仅契约确有变更时重新生成 | 后端契约 | OpenAPI 漂移核对 |
| Database | 涉及 | 备份恢复、开发与测试库升级、结构核对 | 精确目标与恢复证据 | Alembic 与真实 PostgreSQL |
| Deployment | 不涉及 | 不发布部署 | 无 | 不执行 |
| Documentation | 涉及 | 字典、架构、计划、索引及交付记录 | 实际结果 | Markdown、文本、文档、工作区和边界 |

## 7. 影响文件

- 执行前修正的迁移 `apps/backend/alembic/versions/20260923_02_financial_integrity_audit.py`、审计和订单 ORM 元数据，以及受影响的 Backend 测试。
- 已有修改的 `PROJECT_INDEX.md`、`docs/README.md`、`docs/architecture/database-schema-guide.md`、`docs/architecture/commerce-backend.md`。
- 本计划、`plans/INDEX.md`、`CHANGELOG.md`；先前计划保留当时结果，仅补充后续证据指针。

## 8. 风险与恢复

- 仅允许本机 `pinjie_mall_dev` 与独立 `_test` 库；不删除或重置开发数据。
- 新约束遇到无法解释的存量事实立即停止，不猜测审计主体、版本或财务归属。
- 财务迁移不执行破坏性降级；升级前保留经恢复核验的加密备份，必要时按明确授权前向修复或恢复。
- 测试临时目录使用系统临时目录，关闭 Python 字节码写入并隔离 pytest/coverage/静态缓存；原有缓存和 `.coverage` 保留。
- 仅清理本次明确创建的测试库、文件和进程；开发库备份保留为恢复材料。

## 9. 验证与实施结果

- 启动时工作区已有四份文档修改，均为本会话已核对内容。
- 配置安全核对：环境为 local，开发与测试 PostgreSQL 为本机不同库、不同角色；Redis 开发与测试分别使用同一本机实例的 0 与 15 库。
- 确认两个库原为 `20260922_07` 且新迁移尚未部署后，将 `20260923_02` 的审计版本重复检查移到新增列之后；执行后不再改写该 revision。ORM 补齐审计列注释，订单接受支付复合外键使用 `use_alter=True`，消除结构漂移和双向外键排序警告。
- 本机实际 PostgreSQL 为 18.1，Python 为 3.14.6。未升级数据库或依赖；验证证据限于本次实际版本。
- 迁移前分别备份开发库和测试库，使用当前 Windows 用户 DPAPI 加密保存。独立恢复库对比版本、表清单、逐表行数、数据摘要和约束均一致，恢复库自动删除。恢复材料保留于 `%LOCALAPPDATA%/pinjie-mall/backups/20260923-a4856f99070f484f8bb4cb1248518c98`，含备份摘要及完整源版本追溯清单。
- 两个库均升级至 `20260923_02`，重复升级和 `alembic check` 通过。业务表 67 张，含 Alembic 版本表共 68 张；约束 1075 个且未验证约束为 0。迁移前后开发库总行数 126、测试库 2977，除新增审计列和版本元数据外逐表数据摘要保持一致。
- 仓库 `scripts.verify_local_database_recovery` 的空库升级、重复升级、无漂移和备份恢复演练通过，独立临时库自动删除。
- 测试启动曾因临时脚本禁用 cacheprovider 与严格 cache_dir 配置冲突失败，改为隔离缓存目录后恢复。首轮完整测试 416 通过、18 失败，覆盖率 89.30%；据失败证据修正旧测试缺少的契约字段、会员档案时间和过时断言，补充资金分层、异常支付退款、退款补偿回滚及幂等的真实 PostgreSQL 回归。不降低覆盖率、不跳过失败用例。
- 最终完整 Backend pytest 441 项通过，行与分支综合覆盖率 90.06%。新增回归证实：退款资金成功与补偿分离，补偿失败回滚业务变化但保留资金事实，重试不重复回补；重复、取消后迟到与到期临界支付只为异常支付建立退款；未决支付阻止资源释放并仅排入查询任务。
- Backend Ruff、格式、Mypy、导入边界、编译、应用导入、根 OpenAPI 导出漂移和 `scripts.verify_target_model` 均通过。未修改公开接口，根 OpenAPI 无漂移，无需重新生成 API Client。
- 最终数据库核验确认开发库未变、测试库恢复到 2977 行基线、两个库均为 `20260923_02`，且无临时恢复数据库残留。加密备份按第 8 节保留；任务临时目录的精确删除命令被平台策略拒绝，目录不在仓库中，保留为明确清理遗留。

## 10. 剩余问题

真实渠道、小程序认证、常驻 Worker 与前端验收保持既有专项边界。本计划已完成，未执行前端重型测试、真实渠道联调、生产迁移、推送、PR、发布或部署。任务临时目录待平台允许精确清理。
