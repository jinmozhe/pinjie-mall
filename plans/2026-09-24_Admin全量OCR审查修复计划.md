# Admin 全量 OCR 审查修复计划

## 当前状态和结果

- 状态：已结束
- 结果：已完成；OCR 预览选中 93 个文件并完成 100% 静态阅读覆盖，修复商品编辑字段重置、图片上传竞态、库存重复分页、候选值版本推进、积分调整幂等键、对账必填字段、推荐网络缓存串数据、会员与分佣条件字段丢失、查询错误静默及登录跳转边界等问题；Admin typecheck、lint、Markdown、文档治理和文本检查通过。Vitest、pytest、production build、Playwright、浏览器自动化、数据库迁移按项目策略未执行。
- 关联需求：无。本次修复既有管理端实现缺陷，不新增产品能力。

## 目标、范围与授权

- 用户授权：2026-09-24 要求“使用 OCR 帮我审查 Admin 管理端，并修复检测到的问题”。本计划登记后直接执行。
- 使用 OCR `scan --preview` 枚举 `apps/admin`，通过 `delegate rule` 解析规则，由 Codex 完整阅读与核实缺陷。
- 保留现有 Ant Design/Umi 体系、业务操作和生成契约；Backend 及 OpenAPI 作为只读调用链证据。
- 不涉及 Git 提交、推送、发布、部署、依赖升级或重型验证。

## 现状与方案

- 开始时 Git 工作区干净，无匹配活动计划。
- OCR 预览 95 项，默认可审查 75 项，排除 20 项；补充 18 个文本、配置和测试文件，共选择 93 项，排除两个二进制图标。Git 清单无枚举缺口，四批规则解析均覆盖全部请求文件。
- 按共享请求与组件、商品、会员分佣、交易、系统和身份管理、测试配置分批阅读，临时台账记录 SHA-256 与覆盖状态。
- 核实触发条件、现有防护和后端契约后逐文件修复，优先避免数据丢失、重复写入、静默失败及错误操作目标。

## 规则落实与验证

| 来源 | 适用约束 | 落实位置 | 验证方式 |
| --- | --- | --- | --- |
| Admin AGENTS 与工程标准 | 请求错误明确展示、同步防重、失败保留目标与草稿 | 页面、表单与公共组件 | 源码复核、typecheck、lint |
| 认证授权与错误模型 | 权限默认拒绝、Query 缓存身份与实际查询参数一致 | Access、查询键、权限入口 | 调用链静态审查 |
| 根规则与开发流程第 17 节 | 不改生成文件、保留无关字段、限制验证范围 | API 适配、编辑载荷、测试资产 | 契约只读核对、差异检查 |
| 计划规范 | 永久登记、结束回写、准确区分静态和动态证据 | 本计划及索引、Changelog | Markdown 与文档门禁 |

## 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 不涉及 | 只读核对现有语义 | 实际源码 | 静态调用链 |
| Admin | 涉及 | 全量静态审查及确认缺陷修复 | OCR 规则与生成类型 | typecheck、lint |
| Web | 不涉及 | 永久冻结 | 无 | 不启动、不验证 |
| API Client | 不涉及 | 只读使用生成契约 | 根 OpenAPI | 类型核对 |
| Database | 不涉及 | 不迁移、不写数据 | 无 | 不执行 |
| Deployment | 不涉及 | 配置只读审查 | 无 | 不发布、不部署 |
| Documentation | 涉及 | 计划、索引与交付记录 | 修复事实 | lint:md、check:documents |

## 实施顺序与影响文件

1. 完成全部选中文件的 OCR 规则与源码审查。
2. 修复确认缺陷并保留现有回归测试资产。
3. 运行适用轻量门禁，复读差异和文件摘要，清理本次临时产物。
4. 回写具体影响文件、发现、验证与剩余风险，结束计划。

影响文件：`apps/admin` 中已确认存在缺陷的源码及相关测试；本计划、`plans/INDEX.md`、`PROJECT_INDEX.md` 与 `CHANGELOG.md`。

## 风险与回滚

- 异步表单和缓存改动可能影响重试及关闭行为，保留失败草稿和稳定目标，追加针对性测试源码。
- 逐文件补丁保留既有内容，回滚仅撤销本任务差异，不覆盖后续用户修改。
- 动态交互、浏览器显示和真实后端联调没有本次明确授权，静态验证不代表这些场景通过。

## 验证清单

- Admin typecheck、lint。
- Markdown、文档登记和文本检查；必要边界检查按实际变更选择。
- Vitest、pytest、production build、Playwright、浏览器自动化、数据库迁移：按项目策略未执行。

## 待确认问题

无。

## 实施结果与剩余问题

### 覆盖与发现

- OCR `preview_total=95`、`reviewable_count=75`、`excluded_count=20`；排除项为两个二进制图标及 OCR 默认过滤的 18 个文本、配置和测试文件，后者已补充纳入；`selected_files=93`、`reviewed_files=93`、`skipped_files=0`、`coverage_rate=100%`。
- 可靠发现集中在商品编辑未携带品牌与限购字段、商品图片上传期间可提交、库存商品分页与 SKU 展平重复分页、候选值写入后属性版本未推进、积分请求重试更换幂等键、对账创建缺少 `record_type`、推荐网络查询键未包含固定下钻条件、会员与分佣条件字段互相残留或将零值转空值、查询失败被当成空数据、会员权限父级遗漏资格条件权限、登录 redirect 边界和错误响应未收窄。
- 本次未修改生成 API Client、Backend、数据库、Web 或依赖；未新增产品能力。`apps/admin/node_modules/.cache` 与 `apps/admin/src/.umi` 为验证前已存在缓存，按规则保留。OCR 临时台账位于系统临时目录，精确清理命令被当前平台删除策略拒绝，未继续绕过。

### 验证结果

- 通过：`pnpm --filter @pinjie/admin typecheck`、`pnpm --filter @pinjie/admin lint`、`pnpm lint:md`、`pnpm check:documents`、`pnpm check:text`、`git diff --check`。
- 未执行：Vitest、pytest、production build、Playwright、浏览器自动化、数据库迁移和服务启动，遵循项目当前授权与 Admin 规则。
