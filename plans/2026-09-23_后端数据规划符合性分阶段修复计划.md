# 后端数据规划符合性分阶段修复计划

## 1. 当前状态和结果

- 状态：实施中。
- 结果：不适用。
- 当前阶段：A 本地实现完成，阶段 B 尚未开始；整体计划继续实施。
- 范围：审查清单 F-01 至 F-11、G-01 至 G-03 及相关文档和验证改进，按 A 至 F 阶段执行；G-04、G-05 保留为独立专项。
- 原始审查材料保存在用户指定的 `temp` 目录，正式实施与验证结果以本计划为准，不依赖临时目录参与运行或门禁。

## 2. 关联需求

BASE-COM-002、BASE-COM-006、BASE-COM-101 至 BASE-COM-105、BASE-COM-202 至 BASE-COM-205、BASE-COM-301 至 BASE-COM-305、BASE-ADMIN-005、BASE-API-001、BASE-API-002、BASE-DB-001、BASE-QUAL-001。

## 3. 背景

后端 ORM 已登记 67 张表，但静态审查发现资金事实恢复、异常收款、支付与取消裁决、售后与履约互斥、下单佣金冻结、受益资格、零折扣、结算等待期、报价快照、积分幂等及部分数据库约束尚未符合数据字典。接单、消费和邀请资格来源、对账人工处置也缺少完整业务入口。

## 4. 目标

1. 按数据字典落实已确认的内部业务不变量，并保留历史事实及领域边界。
2. 分阶段交付可独立评审的改动、必要回归用例源码、轻量门禁和文档记录。
3. 涉及 API 时同步唯一 OpenAPI 与生成客户端；涉及模型时提供新增迁移与存量校验、恢复边界。
4. 分开记录本地实现、动态验收、真实渠道和实际数据库升级状态。

## 5. 非目标

- G-04 的真实渠道、商户凭据、小程序 Bearer 会话、常驻部署及生产告警专项。
- G-05 的积分到期、兑换、抵扣以及消费钱包入金和组合支付政策。
- 新增 Admin 页面、小程序工程、Web 开发或任何 Web 服务。
- 推送、PR、发布、部署和实际数据库迁移；重型测试没有本任务的点名授权。阶段 A 本地检查点提交已获追加授权。

## 6. 现状分析

| 编号 | 当前问题 | 实施归属 |
| --- | --- | --- |
| F-07 | 零折扣被真假值回退为 1 | A |
| F-10 | 积分同键不比较请求意图，首次并发没有稳定串行边界 | A |
| F-04、G-01 | 未决售后不阻断履约，缺少正式接单命令 | B |
| F-05、F-06、F-08、F-09 | 冻结时点、受益资格、一级兜底、结算期和规则快照不符 | C |
| F-01、F-02、F-03 | 资金和补偿共用事务，异常退款和未决支付裁决缺失 | D |
| F-11、G-03 | 审计及复合归属约束、人工对账处置未完成 | E，接单事件约束前置到 B |
| G-02 | 只有积分调用资格事件，消费与邀请未接通 | F |
| DOC-01、TOOL-01 | 文档状态过期，表数检查不足以证明业务符合 | 随相关阶段同步 |

## 7. 方案设计

### 7.1 规则、落实位置与验证

| 来源 | 约束 | 落实位置 | 验证方式 |
| --- | --- | --- | --- |
| 根与 Backend AGENTS、工程标准 | 最外层用例拥有事务，仓储只查询和写入准备 | 领域服务、应用服务及仓储 | Ruff、Mypy、导入边界、差异复核 |
| 数据字典 7、8、9 章 | 快照时点、资金事实与补偿、固定锁顺序 | 订单、生命周期、分佣、任务 | 回归用例源码及授权后的 PostgreSQL 验证 |
| 认证与审计文档 | 精确权限、事务内授权及审计 | Router、管理用例、审计模型 | 契约、权限静态核对及回归用例 |
| 测试策略 | 重型验证点名授权，未执行不能写通过 | 每阶段验证记录 | 默认轻量门禁，不运行 pytest 或迁移 |
| 计划规范与文档规则 | 同一计划、永久索引、状态同步 | 本计划、两个索引、字典及变更记录 | Markdown、文本、文档及边界门禁 |

### 7.2 阶段 A 设计

- 合法零折扣按零计算；数据损坏导致 discount 规则缺少因子时明确失败，不用原价掩盖配置错误。
- 积分重放比较用户、操作、原始调整量、冲销来源、说明和管理员来源；原始量由不可变账本的可用与债务增量还原，不能把实际可用变化误当请求量。
- 按幂等键、用户账户顺序取得事务级 advisory lock，保护同键重放及无账户时的首次并发；账本保持唯一约束和不可变语义。
- 使用现有事务审计入口；新增针对边界和并发的回归用例源码，按策略不执行 pytest。

### 7.3 后续阶段设计边界

- B：接单和履约与退款共用订单锁；精确权限、管理员事件与契约同步，拒绝越权及非法状态。
- C：下单冻结政策、买家状态及来源，成交固定受益资格，原政策决定等待期；价格和运费快照保存原规则与版本。
- D：资金事实与任务同事务，后续业务幂等恢复；重复和迟到付款建立独立退款，超时不能盲目释放资源。
- E：新迁移先检查真实关联证据，不伪造历史主体与外键；对账处置独立于资金事实。
- F：消费和冲销绑定原成交事实；有效邀请触发点若缺少权威定义，暂停该依赖并继续独立修复。

## 8. 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 涉及 | A 至 F 业务修复及必要用例源码 | 当前实现、领域规则和字典 | Ruff、格式、Mypy、导入边界、编译、应用导入、契约 |
| Admin | 条件性涉及 | 仅新增或变更契约实际影响的现有消费者适配 | Backend 和生成客户端 | 如改动则 typecheck、lint |
| Web | 不涉及 | 永久冻结 | 无 | 不启动、不构建、不测试 |
| API Client | 条件性涉及 | API 变化后生成 | 唯一根 OpenAPI | 生成及漂移核对 |
| Database | 涉及 | B、E 等新增迁移及模型约束 | 存量校验与回滚边界 | 静态复读；实际升级和动态校验未授权 |
| Deployment | 不涉及 | 不执行真实渠道或常驻部署 | 独立专项 | 未执行 |
| Documentation | 涉及 | 计划、索引、字典及交付事实 | 实际阶段结果 | Markdown、文本与治理检查 |

## 9. 实施顺序

| 阶段 | 范围 | 状态 | 交付条件 |
| --- | --- | --- | --- |
| A | F-07、F-10 | 本地实现完成 | 零折扣、积分完整意图及并发建账保护已修复；34 个参数化回归场景已编写，pytest 未执行 |
| B | F-04、G-01、接单事件约束 | 未开始 | 接单及售后互斥、权限契约和迁移源码 |
| C | F-05、F-06、F-08、F-09 | 未开始 | 佣金与报价冻结、资格、预算和结算规则 |
| D | F-01、F-02、F-03 | 未开始 | 内部资金恢复与异常退款闭环 |
| E | F-11 其余项、G-03 | 未开始 | 归属约束、提现审计与人工处置 |
| F | G-02 | 未开始 | 明确政策范围内的资格贡献和冲销 |

## 10. 影响文件

- 阶段 A：`app/domains/membership/service.py`、`app/domains/points/service.py`、`app/domains/points/repository.py` 及对应测试；路径均位于 `apps/backend`。
- 后续阶段：订单、生命周期、分佣、政策、持久任务、管理权限与 API、模型和 Alembic；进入阶段时按当前实现细化。
- 文档：本计划、`plans/INDEX.md`、`PROJECT_INDEX.md`、`CHANGELOG.md`、相关数据库字典段落。
- 生成契约：仅实际 API 输入变化时更新根 `openapi.json` 和 `packages/api-client/src/`。

## 11. 风险与回滚

- 精确逐文件补丁，保留原有配置和领域逻辑；无关改动不纳入。
- A 不变更结构，可回退本阶段代码补丁；不得回滚用户或其他阶段修改。
- 后续涉及资金事实或迁移时，不通过删除成功事件、补造主体或改写已部署 revision 回退；迁移应显式校验存量，不可证明安全时停止依赖步骤。
- 本次不写数据库；生成迁移的实际升级、回滚和生产数据修复另按准确授权执行。
- 验证前记录缓存归属；优先关闭可选缓存或写入独立临时目录，仅清理本次新增且可核对的资源。

## 12. 验证清单

- [x] 阶段 A Backend Ruff 与格式检查。
- [x] 阶段 A Backend Mypy、导入边界、源码编译、应用导入及 OpenAPI 检查。
- [x] 阶段 A 新增回归用例源码复读，pytest 未执行。
- [x] 阶段 A OpenAPI 导出无漂移，公开契约及消费者未变化，无需重新生成 API Client。
- [x] 阶段 A Markdown、文本、文档、workspace 和 boundaries 适用检查。
- [x] 阶段 A Git 差异及状态核对。
- [ ] 本次独立验证目录清理，命令被平台执行策略拒绝，保留情况见第 16 节。
- pytest、数据库迁移和并发动态验证：按项目策略未执行。

## 13. 待确认问题

阶段 A 至 E 的本地实现没有阻塞性未决问题。阶段 F 的有效邀请语义进入该阶段时从权威原文核对；无法确定时只暂停该来源接入。G-04、G-05 为独立专项，不纳入本计划完成范围。

## 14. 用户确认记录

2026-09-23 用户明确要求：“现在按照修复清单开始进行实施，按阶段实现。确认授权执行”。该授权覆盖本地分阶段修复、必要文档与默认轻量验证；不因阶段切换重复确认。未包含 Git、重型测试、实际数据库升级或外部部署动作。

2026-09-23 用户追加要求：“请创建本地检查点提交后继续阶段B的实现。”本次检查点只纳入已完成并经轻量验证的阶段 A 内容；随后继续阶段 B，不包含推送或其他远端动作。

## 15. 实施结果

阶段 A 已完成以下本地实现：

1. F-07：零折扣按零计算，缺少折扣因子的损坏配置返回 `503 / CONFIGURATION_ERROR`，保留原定价优先级。
2. F-10：重放核对用户、操作、原始调整量、冲销来源、备注及管理员来源，同键异意图返回 `409 / STATE_CONFLICT`；原始量包含债务增量。
3. 积分首次写入按幂等键、用户账户取得事务级锁，保护同键并发及不同键首次建账；不可变账本读取无需抢占原流水行锁。
4. 新增 16 个折扣报价场景、16 个积分重放意图场景、2 个真实 PostgreSQL 并发场景的测试源码。未运行 pytest，不宣称动态场景通过。
5. `uv sync --locked`、Ruff、格式、Mypy（164 文件）、import-linter（2 个合同）、源码编译、应用导入及 OpenAPI 构造（154 路由）通过。初次格式检查命中两个本次改动文件，已格式化并复验通过。
6. 阶段 A 未修改公开 Schema、路由、依赖和数据库结构；OpenAPI 导出至独立系统临时目录后与根契约 SHA-256 一致，API Client 不需重新生成。
7. `pnpm lint:md`、`pnpm check:text`、`pnpm check:documents`、`pnpm check:workspace`、`pnpm check:boundaries` 通过，原审查报告定向 Markdown 检查通过。首次文档检查发现活动计划需表格登记，首次 Markdown 检查发现重复 Fixed 标题，均已按原规则修复并复验。
8. 验证缓存隔离在系统临时目录，原有 `.mypy_cache`、`.ruff_cache`、`.import_linter_cache`、pytest 缓存及用户 `temp` 材料保留。未启动项目服务、浏览器、测试数据库或监听端口；未执行提交、推送及数据库升级。

阶段 B 至 F 尚未实施，后续继续使用本计划及已有授权，不重复申请相同修复授权。

## 16. 剩余问题

- 阶段 B 至 F 的实现和验证尚未完成；阶段 A 的动态测试未执行。
- 重型测试及实际数据库校验未执行，不作为轻量阶段通过后的隐式追加步骤。
- G-04、G-05 保持后续专项边界。
- 本次独立验证目录 `C:\Users\soman\AppData\Local\Temp\pinjie-stage-a-c5ea79b00a3149659ba0dab7b7b9ccc1` 保留：223 个文件、39 个子目录，仅包含本次 Mypy、编译缓存及 OpenAPI 导出。删除前已核对归属、绝对路径和无重解析点；平台拒绝清理命令，返回 `blocked by policy`，未提供更细原因。已停止尝试，没有改用其他工具绕过；不影响本地代码与已完成验证。

用户可在自己的 PowerShell 中清理该已核对目录：

```powershell
Remove-Item -LiteralPath 'C:\Users\soman\AppData\Local\Temp\pinjie-stage-a-c5ea79b00a3149659ba0dab7b7b9ccc1' -Recurse -Force
```
