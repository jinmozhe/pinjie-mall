# Web 冻结镜像安全检查修复计划

## 1. 当前状态和结果

- 状态：已结束
- 结果：已完成；补充非 root 用户声明，适用轻量门禁及线上 Security 复验通过。

## 2. 目标、范围与授权

- 关联需求：无。本次修复既有冻结入口的静态安全检查问题，不改变产品能力。
- 用户确认：2026-09-16，用户要求查看并修复 Security 工作流运行 `35074572075` 的失败。
- Git 交付授权：2026-09-16，用户随后显式执行 `$git-sync`，授权提交、推送、PR、检查通过后的 rebase 合并和本地同步；不包含发布、部署、重型验证或降低门禁。
- 远端功能确认：2026-09-16，用户确认 Dependency graph 已启用并要求重试，随后执行失败工作流复验。
- 目标：为冻结 Web Dockerfile 显式指定非 root 用户，同时保留构建和启动阻断。
- 非目标：Web 业务开发、镜像构建、服务启动、依赖升级、远端设置修改、发布部署。
- 待确认问题：无，本地修复已有明确授权。

## 3. 失败依据与方案

[失败的 Security 运行](https://github.com/jinmozhe/pinjie-mall/actions/runs/35074572075) 对应源码 `019c6299a45d75124f8e1017d7ab89198c103733`。该 Push 运行中 Secret scan、Dependency vulnerability scan、Node dependency audit 和 Source state 均成功，只有 Semgrep CE SAST 失败；Dependency review 按既有事件条件未执行。

Semgrep 报告 `dockerfile.security.missing-user.missing-user`，位置为 `apps/web/Dockerfile` 的 `CMD`。在 `FROM` 后补充 `USER node`，使已有两个报错指令在非 root 身份下执行，继续拒绝 Web 镜像构建及启动。不增加扫描排除、规则豁免或失败忽略。

| 规则来源 | 适用约束 | 落实位置 | 验证方式 |
| --- | --- | --- | --- |
| 根 AGENTS.md、开发流程第 17 节 | 精确修改、保留原有行为与用户资产 | Dockerfile 单行补丁 | 复读和 Git diff |
| apps/web/AGENTS.md | Web 永久冻结，禁止构建、启动及业务开发 | 保留 RUN、CMD 主动抛错 | 静态核对，不执行 Docker build |
| SECURITY.md | 非 root、失败关闭、不豁免安全发现 | 显式 USER，Security 工作流保持原样 | 失败日志与补丁逐项对照；后续线上 Semgrep 复验 |
| 根验证规则 | Security 仅在 GitHub Actions 执行 | 本地只运行已配置轻量治理检查 | 记录实际退出码，线上未复验不写成通过 |

## 4. 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 不涉及 | 无源码变更 | 无 | 差异核对 |
| Admin | 不涉及 | 无源码变更 | 无 | 差异核对 |
| Web | 涉及 | 冻结 Dockerfile 指定非 root 用户 | 既有 Node 基础镜像 | 静态核对 |
| API Client | 不涉及 | 无契约变更 | 无 | 差异核对 |
| Database | 不涉及 | 无模型或迁移变更 | 无 | 差异核对 |
| Deployment | 不涉及 | 不构建、发布或部署 Web | 无 | 差异核对 |
| Documentation | 涉及 | 修复计划、索引及变更记录 | 真实日志与补丁 | 文档和文本门禁 |

## 5. 实施顺序与影响文件

1. 登记本计划、`plans/INDEX.md` 与 `PROJECT_INDEX.md`。
2. 修改 `apps/web/Dockerfile`，同步 `CHANGELOG.md`，补充原基线计划的安全验证边界。
3. 运行 `pnpm lint:md`、`pnpm check:text`、`pnpm check:documents`、`pnpm check:workspace`、`pnpm check:boundaries` 和 `git diff --check`。
4. 复读差异、核对临时资源并记录本地结果与线上复验缺口。

## 6. 风险与回滚

本次只改变冻结文件的用户声明，不恢复运行能力。风险为本地静态核对不能替代线上完整规则集扫描。需要回退时仅反向应用本任务对应补丁，保留所有计划资产和后续用户修改；回退 USER 会重新引入本次安全告警。

## 7. 实施及验证结果

- `apps/web/Dockerfile` 已补充 `USER node`，原有 FROM、RUN 和 CMD 保持原样，未修改扫描规则或工作流。
- `pnpm lint:md`、`pnpm check:text`、`pnpm check:documents`、`pnpm check:workspace`、`pnpm check:boundaries` 与 `git diff --check` 通过。
- 首轮 Markdown 重复标题与活动计划索引格式问题已修正，受影响门禁复验通过。
- project-hygiene 的额外链接检查报告 16 处已有失效引用，位于本次未修改的 ADR 与运维文档，主要指向仓库中不存在的历史计划；该辅助检查未通过，不将其写成仓库整体文档质量通过。
- 验证前根目录和 Web 目录已有 `node_modules`；本次未安装依赖、启动服务或生成应用构建及 Python 缓存，既有资源保留，无本次临时文件待清理。
- 本地未运行 Semgrep、Docker build、pytest、Vitest、浏览器或数据库验证。应用源码及 API 契约无变更，未运行无关应用本地检查。
- [PR #3](https://github.com/jinmozhe/pinjie-mall/pull/3) 的功能补丁通过线上 Semgrep；用户启用 Dependency graph 后，[Security 运行第二次尝试](https://github.com/jinmozhe/pinjie-mall/actions/runs/35077577643/attempts/2) 成功，依赖审查、依赖漏洞扫描、Node 审计和密钥扫描均通过。同一功能补丁的 Governance、Backend、Frontend 也通过，后续文档收尾提交及合并状态以 PR 最新检查为准。

## 8. 剩余问题

本次 Security 两项失败已分别通过 Dockerfile 修复和用户启用 Dependency graph 解决，未降低安全门禁。16 处既有文档失效引用仍保留为范围外问题；重型验证、发布与部署未执行。
