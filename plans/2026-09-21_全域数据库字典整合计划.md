# 全域数据库字典整合计划

## 当前状态和结果

- 状态：已结束
- 结果：已完成；目标字典及需求同步完成，适用轻量治理和变更文档检查通过，全仓 Markdown 受既有参考稿及 PR 草稿阻断；运行源码与数据库未迁移。
- 范围：比较两份字典，核对源码与迁移，建立长期目标模型及覆盖证据，同步需求与必要索引。

## 关联需求与授权

- 关联：BASE-GEN-003、BASE-DB-001、BASE-QUAL-001、BASE-COM-001 至 BASE-COM-006、BASE-COM-101 至 BASE-COM-105、BASE-COM-201 至 BASE-COM-205、BASE-COM-301 至 BASE-COM-305。
- 用户要求通读两份字典并结合既有规划优化正式字典，作为后续开发基准。
- 用户已确认采用数据库字典的新目标：平台统一运费、整单含原运费退款、零元成交、最多三级分佣，同步 PRD，现有代码标为待迁移。
- 用户进一步确认每订单行总佣金不得超过来源 S，来源禁用则该商品全部停发；按一级、二级、三级依次分配剩余额度，超额截断。
- 用户确认本期仅未发货实物、未交付虚拟商品可申请整单退款；发货/交付后售后留待专项。本期不启用退货验收或七日售后。
- 本次不修改运行源码、生成契约、迁移或数据库，不执行 Git 提交、发布或部署。

## 现状与目标

原字典混合当前实现、目标模型和后续规划，表数、字段、状态机与现行 PRD 存在冲突。参考 v3.0 补充了端到端流程与并发约束，但大部分字段正文沿用原稿，并存在未定义字段、旧编号及不适用的 Markdown 格式。

以正式字典为唯一目标物理模型文档，保留全部具名实体，补足系统配置、缺失字段、约束、JSON、状态与恢复流程；区分表名覆盖、字段覆盖、运行一致性和业务验收，不以文档完备代替全项目闭环。

## 规则落实与影响矩阵

| 规则来源 | 约束 | 落实位置 | 验证方式 |
| --- | --- | --- | --- |
| 根 AGENTS.md、开发流程第 17 节 | 保留已有修改，先读后改，UTF-8 无 BOM | 正式字典、索引与需求逐文件修改 | 复读、原始副本及差异核对 |
| 产品需求基线、计划规范 | 产品政策变化同任务同步，历史计划永久保留 | PRD、新计划及两个索引 | 需求编号、状态与引用核对 |
| 根验证规则 | 仅文档及治理轻量检查 | 既有 pnpm 检查命令 | Markdown、文本、文档、workspace、boundaries |
| 数据单一来源与契约规则 | 区分目标设计与现行实现，不手改 OpenAPI | 字典与 commerce-backend 导航 | Model、Alembic 源码静态对照 |

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 不涉及源码修改 | 只读核对模型、迁移、服务和配置 | 现有代码 | 静态清单与字段差异 |
| Admin | 不涉及源码修改 | 记录未来消费者迁移边界 | 新目标需求 | 文档核对 |
| Web | 不涉及 | 永久冻结 | 无 | 不启动服务 |
| API Client | 不涉及生成 | 后续按后端导出链同步 | 后续实现 | 本次未生成 |
| Database | 涉及设计 | 全域目标字典、约束和迁移差异 | 两份字典及当前模型 | 静态表/字段核对，不连接数据库 |
| Deployment | 不涉及执行 | 记录迁移与渠道验收边界 | 后续专项 | 不发布部署 |
| Documentation | 涉及 | 字典、PRD、索引、计划与 Changelog | 已确认目标政策 | 适用轻量治理检查 |

## 实施步骤

1. 通读原稿和参考稿，核对需求、历史计划及源码。
2. 按物理表名去重，核对当前模型和迁移覆盖，记录未实现模块。
3. 完善正式字典字段、全局配置、约束、状态机、JSON、幂等、事务和闭环矩阵。
4. 同步目标 PRD、小程序需求引用与现行架构文档的迁移边界。
5. 运行适用检查，复读差异并回写结果。

## 风险与回滚

目标字典包含新政策及尚未实现能力，后续需专项完成迁移、接口和消费者适配，不能直接当作现行数据库结构执行。原字典与 docs/README.md 含用户未提交修改，编辑采用逐文件补丁；需要机械转换时先保存原字节并校验，禁止覆盖用户新增修改。无需运行数据回滚；文档回退只撤销本次差异。

## 验证清单

- [x] 两份来源与源码覆盖核对：原稿 66 表、目标 67 表、785 个字段条目，当前 Model 与迁移建表集合均为 44 表；原稿与当前表名全部登记。
- [x] 目标字段、状态、JSON 和流程引用核对：67 表均有约束登记，无重复字段，JSON 示例可解析，本地链接存在；现行六个退役/迁移字段在差异章节登记。
- [x] 需求、索引和现行实现边界同步，所有关键业务决策已确认。
- [x] pnpm check:text、check:documents、check:workspace、check:boundaries 通过；工作区 ready 输出只代表结构门禁，不代表业务验收。
- [x] 修改的 10 份 Markdown 定向检查零告警，project-hygiene 文本卫生零错误，git diff --check 通过。
- [ ] pnpm lint:md 全仓未通过：既有 temp/database-schema-guide-v3.0.md 的 604 条格式问题与 .git/codex-git-sync/20260919-012500/pr-body.md 的 6 条多一级标题问题，本次不改参考原稿、不改门禁忽略规则。
- [x] 修改复读及范围检查；未启动服务、未创建项目缓存，保留用户原有参考稿、HTML、缓存及历史计划。
- 未执行：pytest、Vitest、build、浏览器验证、数据库迁移、真实渠道联调和生产验收。

## 待确认问题

无。目标政策、来源预算及分配顺序、售后范围均已由用户确认。

## 实施结果与剩余问题

正式字典保留原有全部实体，补齐 system_settings 完整字段及小程序身份；区分 61 张目标运行表、1 张存量模板表和 5 张后续规划表。新增配置、约束与索引登记、JSON 契约、零元原子成交、支付事实和业务成交分段恢复、整单退款补偿、三级预算分配、钱包账链、任务租约、三类对账、38 项验收与迁移映射。PRD、小程序需求、现行架构入口及 Changelog 同步。

表名登记覆盖原稿 66/66、当前仓库 44/44，不能推导运行数据库 100% 物理一致。当前代码的商品运费模板、已交付明细部分退款、正额限制和固定两级佣金均待迁移；真实支付/退款/提现、Worker 运行、小程序及完整验收尚未完成，整个项目尚未闭环。后续积分/资格五表仅保留规划边界，发货/交付后售后另设专项。

未提交 Git，未修改源码、Alembic、OpenAPI 或生成客户端。为保护任务开始时未跟踪字典及已修改索引的原文，原字节恢复副本保留于系统临时目录 `C:/Users/soman/AppData/Local/Temp/pinjie-schema-audit-63aa1f6871bb4be29a19d7ae99c8f816`；属于本次明确保留的恢复材料，未写入仓库。静态盘点中间文件收尾删除。
