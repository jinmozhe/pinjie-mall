# apps/web 已冻结

本目录是历史代码骨架，不属于 `pinjie-mall` 的交付范围。

- 禁止新增或修改业务逻辑、页面和依赖。
- `dev`、`build`、`start` 与 `test` 命令会明确失败。
- 禁止启动服务、构建镜像、发布镜像、部署容器或监听 3000 及其他 Web 端口。
- 仅保留现有静态源码、类型和语法检查，以维持 Monorepo 治理兼容。

后续消费者端仅以微信小程序形态实施，不复用本目录。
