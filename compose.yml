# 本地开发使用。
# 只启动 Redis，PostgreSQL 直接使用本机安装的实例。
# 启动命令：docker compose up -d
services:
  redis:
    image: redis:8.10.0-alpine
    ports:
      - "6380:6379"  # 宿主机 6379 已被占用，本地开发使用 6380
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
